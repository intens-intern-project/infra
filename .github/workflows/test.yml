name: Test workflow
on:
  workflow_dispatch:

jobs:
  first-and-only-job:
    runs-on: iip-org-runner
    steps:
    - name: Say hello
      run: echo "Hello from local runner"

    - name: Install kubectl
      uses: azure/setup-kubectl@v5.1.0
      with:
        version: v1.36.1

    - name: Test if kubectl is working
      run: kubectl get all -n iip