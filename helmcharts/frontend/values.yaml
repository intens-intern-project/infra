replicaCount: 1

chart:
  name: frontend
  namespace: iip

image:
  repo: ghcr.io/intens-intern-project/frontend
  tag: "latest"
  imagePullPolicy: Always

imagePullSecrets:
  - name: github-packages-secret

service:
  type: ClusterIP
  port: 80

environment: dev